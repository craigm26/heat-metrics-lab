// src/components/work-rest-table.js
// Ch 8 work-rest table. Mostly a static SVG; init is a no-op for now,
// matching the other component init pattern so it's easy to extend later
// (e.g., row-hover popovers with NIOSH source quotes).

export function initWorkRestTable() {
  // No interactive behavior in v1. SVG is embedded directly in index.html.
}
